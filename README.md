# ENV Var Theater

Find where your environment variables are hiding across files and see what value actually gets used at runtime. Run: `python env_theater.py` or `python env_theater.py VARIABLE_NAME` to search for specific variables.